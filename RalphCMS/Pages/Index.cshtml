@page
@model RalphCMS.Pages.IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container-fluid">
    @if (Model.Page != null)
    {
        @Html.Raw(Model.Page.Content)
    }
</div>

@section Scripts {
    <link href="~/css/wordWrap.css" rel="stylesheet" />
}